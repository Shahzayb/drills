# History

Every plan and implementation, in chronological order. Append-only — new entries go at the
bottom, one line each. A line is added when a plan file is created (status `planned`) and
updated (or a new line added) when the implementation lands (status `implemented`). This is
a log, not a summary — for current state and reasoning see `progress.md` and `techContext.md`.

The description column carries the decision or result that would otherwise take re-reading
the whole plan to recover — not just the topic. See `SKILL.md` for what that means in practice.

| Date | Plan | Description | Status |
|---|---|---|---|
| 2026-08-06 | [workflow-hardening](../plans/2026-08-06_workflow-hardening.md) | 6 gaps in the plan/memory-bank workflow (untriggered record step, plan-mode can't write files, competing memory stores, unenforced caps, untracked dirs, absolute plan gate) fixed with advisory hooks (`Stop`/`PreCompact`/`PostToolUse`) + CLAUDE.md rules; deliberately non-blocking. | implemented (hooks later removed as over-built — not carried into the current CLAUDE.md) |
| 2026-08-06 | [drill-01-health-endpoint](../plans/2026-08-06_drill-01-health-endpoint.md) | Backend wired to Postgres (raw `pg`) and Redis (`ioredis`) — no ORM, no `@nestjs/terminus` (its indicators need TypeORM/Sequelize/Mongoose). `GET /health` runs both probes concurrently under a 2s timeout, 503 if either is down. Gotcha worth remembering: `lazyConnect: true` + `enableOfflineQueue: false` can't combine on ioredis — the first call rejects before it ever connects. | implemented |
| 2026-08-07 | [drill-02-schema-and-migrations](../plans/2026-08-07_drill-02-schema-and-migrations.md) | Five core tables (organizations/users/memberships/conversations/messages), hand-written SQL inside `pgm.sql()` (node-pg-migrate used only as the runner, not its builder). Every tenant-owned row carries `org_id` directly, even where a join could reach it. `conversations.id` is `uuidv7()` — chosen deliberately as a 16-vs-8-byte cost to measure later, not to read about. `messages.org_id` has an FK but no index — a deliberate gap for a future cost drill. Migration 002 drops `subject` to make the rollback lossy on purpose. | implemented |
| 2026-08-09 | [drill-03-conversation-list](../plans/2026-08-09_drill-03-conversation-list.md) | `GET /conversations` + RSC list page, deliberately naive: `LIMIT`/`OFFSET`, no cursor, no composite index, no caching, no joins — so cards 08/09/10 have something real to break. Tenant identity is the `X-Org-Id` header, not a query param, to keep it out of user-controlled input. `ORDER BY updated_at DESC, id DESC` — the `id` tiebreaker is a correctness fix, not an optimization, since `updated_at` alone has ties. Closes with 4 ranked predictions for what breaks first at 2.5M rows. | implemented |
| 2026-08-11 | [drill-04-bulk-seed](../plans/2026-08-11_drill-04-bulk-seed.md) | Deterministic, skewed seed (2.5M conversations / 10M messages, 40/40/20 org skew, template+faker corpus instead of lorem ipsum) loaded via `COPY` inside one `wal_level=minimal` transaction. Biggest single lever, measured: dropping `messages_conversation_id_fkey` (not just indexes) before load, `NOT VALID` + `VALIDATE` after — 157.3s → 105.9s. Result: 104.3s seed against a 6-minute budget, 3.2GB on disk. Checked drill-03's 4 predictions: 3 confirmed as written, the `count(*)` one was half right — its cost depends on whether `VACUUM` (not just `ANALYZE`) has run, which drill-03 had no reason to know. | implemented |
| 2026-08-12 | [seed-simplification](../plans/2026-08-12_seed-simplification.md) | Finishes drill-04's "measured and reverted" pass: reverts the three Node-side hot paths it *kept* (HEX table, day cache, template pre-compiler — 9.7s of CPU), plus the 5-flag lever matrix and the triplicated structure COPYs. The four SQL levers all stay — measured as seconds paid per line removed, the Node-side code is ~0.12 s/line against the FK lever's ~5.1, and the levers are also what the drill teaches. Key property: every change is fingerprint-preserving, so all five recorded table hashes must come back unchanged — a data-identity proof across 12.5M rows, not just a passing test suite. Result: 108.21s vs 108.2s before, all five hashes unchanged, non-comment code 554 → 472 lines. The bench confirms the mechanism — generation fell from ~2M to 1.11M bodies/s and stayed 8.7× ahead of Postgres's 127k rows/s, so 9.7s of CPU bought ~0s of wall clock. Watch out: the first run read 114.24s and a single measurement would have reported a 5% regression that does not exist. | implemented |
| 2026-08-13 | [drill-05-load-test-baseline](../plans/2026-08-13_drill-05-load-test-baseline.md) | k6 baseline for `GET /conversations` — the `before` column cards 08/09/10 get compared against, from the client's side rather than `EXPLAIN`'s. 10 VUs closed-loop, 20s warm-up excluded via a second scenario, 60s measured, 3 runs each. Whale (org 1) p50/p95/p99 **175.5/340.3/375.4ms at 48.6 req/s**; tail (org 150) **2.14/2.93/4.11ms at 4,415 req/s** — 82–116x apart. Two findings beat the table: drill-03's prediction #2 was **wrong** (`count(*)` is 33ms post-vacuum, the *faster* half; the 102ms list query with no `(org_id, updated_at DESC, id DESC)` index is the real cost, so the whale is card **09**'s problem not card 08's), and `Promise.all` of two queries takes **2 pool connections per request** — 20 wanted from `max: 10` at 10 VUs. Within-sweep noise floor 1.9–7.2%, but the sweep was run 3x in one evening and the *medians* moved up to 14% — and monotonically (whale p95 323→333→340ms, throughput 50.6→49.3→48.6), i.e. the laptop drifted ~4% slower over 90 min. So: refuse anything under ~15% (20% on the tail), and **interleave A/B in one sitting** rather than trusting an across-sitting comparison. Checked the load generator wasn't the bottleneck (20 VUs: latency 2.3x worse, throughput *down* 4%). | implemented |
| 2026-08-13 | [drill-06-request-id-propagation](../plans/2026-08-13_drill-06-request-id-propagation.md) | One request id threaded browser → Next → Nest → Postgres/Redis, all logs converted to structured JSON via pino + nestjs-pino. Correlation, **not** tracing — no spans, no sampling; the OTel stretch is scoped as a separate phase in the same plan file and must land after the A/B below, because auto-instrumentation moves latency. Four decisions worth keeping: the id is memoised on the request with a symbol key so `genReqId` and our middleware can't disagree about ordering; an inbound `x-request-id` is allowlisted `^[A-Za-z0-9_-]{8,64}$` because it lands in a SQL comment and `pg` sends param-less queries (e.g. `/health`'s `SELECT 1`) over the *simple* protocol, where `;`-separated statements are legal — the injection is real, not theoretical; the ALS context is entered in **middleware, not the interceptor**, because `next.handle()` is a lazy Observable that subscribes outside any `als.run()` wrapping the pre-phase; and `application_name`/`CLIENT SETNAME` were rejected as per-connection on pooled singletons (2 extra round trips per request). Postgres statement logging is a runtime toggle (`db:log:on`/`off`), not always-on, so drill 05's instrument is unchanged — watch out, `ALTER SYSTEM` persists in the volume across restarts. Checked rather than assumed: Next 16 renamed `middleware.ts` → `proxy.ts`; `forRoutes('*')` *is* fine on Nest 11 (`legacy-route-converter.js` rewrites it to `{*path}` and suppresses the warning); `LoggerModule` is `@Global()` so re-importing it doubles every log line. **Result:** one grep returns 12 lines across 3 services; 27 e2e tests pass. Drill 05's finding #2 confirmed and upgraded from inference to observation — the two `Promise.all` queries of one request are executed by two different Postgres backend PIDs. The overhead is a **fixed ~0.9ms/request, not a percentage**: tail org (2ms requests) −26.7% throughput with logging *silent*, −30.7% info, −34.7% debug; whale org (176ms requests) **no measurable effect at all** — "before" landed between two "after" runs on every metric, and those runs reproduce drill 05's baseline (48.02 vs 48.57 req/s). So the plumbing costs ~4x what the logging costs, and a percentage overhead is meaningless without saying what it is a percentage of. Biggest self-inflicted bug, found by measuring rather than reading: **a suppressed log line still evaluates its arguments** — `summarise(text)` ran a regex per statement at every level, including `silent`; `isLevelEnabled` guards recovered 6.0%/5.0% and, as predicted, 0% at debug. Review caught four more: pino-http logs every 5xx at `info` without `customLogLevel`; `customProps` is applied twice so a mid-request field emits a duplicate JSON key with two values; `exclude` drops the request context where `autoLogging.ignore` does not; and a thrown query logged as `db_query` with `rows: null` reads as an empty result set. | implemented |
